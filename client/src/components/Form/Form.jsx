import React from "react";
import useStyles from './styles';

const Form = (props) => {
    const classes = useStyles();
    
    return (
        <form>
            FORM
        </form>
    )
}

export default Form;