import React from "react";
import useStyles from './styles';

const Post = (props) => {
    const classes = useStyles();
    return (
        <div>
            Post
        </div>
    )
}

export default Post;